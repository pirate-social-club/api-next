# Creator provider-choice projection

The locked creation loader now exposes both accepted provider identifiers from
the nationality requirement state. The public contract requires the exact pair
and confines the current provider to it. No provider order is a preference.
Client 0.77.0 is a new immutable local artifact; older artifacts are unchanged.

The full static check passed. The five-test creator flow PostgreSQL suite
passed against a task-owned socket-mounted PostgreSQL 17 with one fresh marker,
and its wrapper exited zero. The container and socket directory were removed.
Six contract tests passed. These are focused proofs, not a repeated full runtime
or staging acceptance run. The latest full runtime evidence belongs to 0.76.0.

The first static check found a widened string literal in a new test fixture;
that was corrected before the passing check. An initial generator invocation
from the package directory reported Script not found; generation was rerun from
the repository root before packing the final artifact. Retained logs have ANSI
sequences and trailing terminal whitespace removed, with outcome lines intact.
