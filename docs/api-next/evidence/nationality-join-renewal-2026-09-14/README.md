# Nationality join renewal and evidence enforcement

Eligibility now preserves the bound provider across refetches. Unusable evidence after a satisfied ceremony issues a new action while the historical satisfied state remains immutable. A changed provider binding retires the old pending action. The shared loader can select a valid alternative and can filter to the provider pinned by a quote. Enforcement locks the proof session, subject binding and assertion, then rechecks the exact assertion before allowing the transaction to proceed. A current nationality policy with zero or one binding row fails closed rather than appearing absent.

The final full check passed with 176 migrations and client 0.75.0 unchanged. PostgreSQL passed 37 tests across the nationality, general Gates community, creation nationality flow and ceremony-store suites, with four fresh markers. Its wrapper exited zero and removed its task-owned container and socket directory. The complete serial runtime chain passed 4,137 unit tests, 20 Node tests and workerd 82/74/2/10/15. That chain began before the last exact-assertion selection tightening; the final 37-test PostgreSQL rerun and full check include it. No later full runtime-chain rerun is claimed. Sentinel upload shards were recomputed and their unit check passed.

A new integration gap was found while preparing handle checkout: nationality resolver provider switching issues a new child ceremony, but startVerification reserves and revalidates the original caller intent. Existing PostgreSQL start assertions exercised the resolver directly rather than the entire start service. Fixing that canonical-intent handoff with actual start-service proofs is next; this checkpoint is not complete provider-choice acceptance.

Handle quotes and claims, buyer ceremonies, browser flows, adult viewing, metadata and production acceptance remain unfinished. Nothing was enabled, advertised, pushed or deployed. The owner-approved one-year nationality lifetime is settled and remains explicit configuration.
