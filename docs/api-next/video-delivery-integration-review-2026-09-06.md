# Delivery integration review checkpoint

Reasons PR 288 merged as 66b58debe3d7c305cf958545ad7ec98a1a8ad983. Delivery rebased once onto that source from preserved tip 32dcaae036a3c03550bedb77b311eba7c39eb8d4. The registered pre-rebase capture contains complete history. No delivery PR is authorized before the workspace_owner reviews the clean integrated branch.

Fetched origin contained publication reasons 0125 and HNS execution privileges 0126. Delivery therefore uses 0127_video_delivery_ingest.sql, superseding the earlier 0125 and 0126 delivery reservations. The checksum changed with the migration header; regenerated schema.sql and test-reset.sql were byte-identical to the rebased artifacts. The foundation inventory explicitly checks 0127 and refuses the old delivery name. Both preflight refusal guards remain: existing non-not_started ingests and running enrichment rows require disposition.

Rebase conflicts were in migration checksums, the working client version/provenance, exact waivers and the release ledger. Both immutable artifacts 0.63.0 and 0.64.0 were preserved unchanged. The merged reason-code contract and publication event-sequence fence remain intact. Delivery's two direct publication test fixtures now pass the event sequence returned by their committed decision; no production publication fence was weakened.

Client 0.65.0 is the one Solid adoption. It carries membership_required, provider_submission_unconfirmed, playback and thumbnail unavailable states, and the playback-access and authorized poster endpoints. The six exact waivers are delivery response changes against 66b58debe3d7c305cf958545ad7ec98a1a8ad983; already-landed reason waivers are removed. The two new endpoints are additions and require no breaking-change waiver under the detector. The older 0.63.0 handoff is historical candidate evidence and its mention of merged reasons is superseded by this verified combined cut.

Validation is local and uses fixtures. The first focused PostgreSQL run exited 1: 14 foundation tests passed and ten delivery tests failed because their publication fixture omitted observedEventSequence. The first check exited 1 with those two missing-field errors. After adding the observed sequence, all ten delivery PostgreSQL tests passed (81 assertions, exit 0). The focused unit set passed 205 tests; the full unit suite passed 3202 tests and 14083 assertions; Node passed 20 tests. The check rerun exited 0, verified 127 migrations and verified the immutable 0.65.0 package. Baseline generation and the exact-baseline breaking check exited 0. Full PostgreSQL and Workerd results follow after completion; these pending gates are not acceptance claims.

No deployment, secret mutation, hosted Stream or Qencode call, or video enablement occurred. The staged readiness chain remains in video-original-audio-staging-proof. Source access hostname/DNS/certificate, credentials, the bounded Qencode fixture, delivery review/integration, and Solid playback/browser acceptance remain separate prerequisites. Execution is parked; reservation lifetime is unratified and cross-store platform-hold tooling remains a follow-up. Participation opens only after the combined Solid adoption and agreed draft ownership.

The ordinary suites completed with exit 0: 3202 unit tests, 20 Node tests, and 160 Workerd tests (80 general, 55 HTTP, 2 Self, 9 HNS verifier, 14 source gateway), run serially with maxWorkers 1. The source-gateway run logged a pump-canceled disconnect during its stream tests and still passed all tests; that diagnostic remains in the retained log.

An older draft PR 273 already exists at pre-rebase source 32dcaae0. This integration has not pushed to or updated that draft, opened another delivery PR, or requested remote checks. The clean local review checkpoint is held for the workspace_owner before any delivery PR update or publication.

The initial full PostgreSQL run passed isolated (35), general shard 0 (103), shard 1 (98) and shard 2 (101). Shard 3 exited 1 with 95 passes and the composed wrapper timing out at 120 seconds. A direct Workerd run retained all child output and ended with 29 passes and one entering-wait timeout; full swap, slow database statements and a broken pipe were observed during that run while another lane ran PostgreSQL. This is retained as a failed run, not classified as a proven product defect or silently discarded.

The composed wrapper now streams stdout/stderr instead of buffering diagnostics until completion and allows five minutes for the complete 30-test serial suite plus startup. Individual tests still time out at 60 seconds. Even excluding the anomalous waiting case, the measured direct run exceeded the previous two-minute aggregate budget. The entire affected shard is rerun with this harness adjustment; other partitions have no source changes affecting them.

The affected shard rerun exited 0: 96 PostgreSQL tests, 676 assertions, 339.30 seconds. Its composed child passed all 30 tests across both files in 116.74 seconds, including the entering-wait approval drill in 2788 ms. Together with the unchanged successful partitions, the full PostgreSQL gate passed 433 tests; the 30 nested Workerd cases are reported separately. The initial failed shard and direct-run failure remain in the capture. No individual timeout or production retry/deadline was changed.

Final check exited 0 after the harness change and verified all 127 migrations, fresh generated contracts and immutable client 0.65.0. The exact-baseline breaking check passed with six delivery waivers. This is the clean local review gate; required remote checks have not been run for the rebased delivery source and remain owed after owner review.

Owner full review accepted this branch for PR publication after two commits. Source grants now use the durable encoding deadline, while the copy intent still refuses expired acceptance; the internal transport port carries both deadlines. Transport/interpreter tests passed 23 cases after updating their exact input fixture. The authorization PostgreSQL suite passed eight tests, including one four-condition case proving anonymous general access, anonymous adult denial, and anonymous/authenticated denial for platform and open review holds. Its first run exceeded Bun's five-second default for four fixtures; the case now has a 30-second bound and completed successfully. Check exited 0.

The authorized Stream fixture must prove the downloads endpoint response for absent downloads and preservation/filtering of the 64-character creator digest. No speculative response interpretation was added. Exhausted delivery operator recovery, the dormant members_only helper replacement, and measuring the six-per-minute source/post playback limit remain documented follow-ups. Those obligations are also in the staging-proof record. The prior review hold is lifted for the reviewed changes; final-head remote checks remain mandatory before merge.
