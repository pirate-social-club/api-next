# Money-admission checkpoint evidence

The first and final focused PostgreSQL runs passed. The recovery extension first failed because its fixture reused an immutable provider receipt; that failed log is retained alongside the corrected run. See ../activity-participation-money-admission-2026-09-06.md for the boundary, exact test names and acceptance limits. These copies normalize trailing whitespace only. The coordinator owns capture and staging-index registration.
