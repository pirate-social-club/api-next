launch=gsqlite3
gsqlite3-database=/var/lib/powerdns/pdns.sqlite3
gsqlite3-dnssec=yes
# local-address and allow-notify-from are host-specific command arguments in
# compose.yaml. This static file deliberately contains no IPs or secrets.
local-port=53
primary=no
secondary=yes
autosecondary=yes
# Unknown zones may be created only from a TSIG-signed notification sent by the
# explicitly registered autoprimary. Unsigned autoprovisioning fails closed.
allow-unsigned-autoprimary=no
# Root-readiness observation runs only on the primary authority host. Permit
# that one source to request a TSIG-authenticated transfer so both retained
# authority views can be compared byte-for-byte; all other AXFR stays closed.
disable-axfr=no
allow-axfr-ips=94.103.168.161
api=no
webserver=no
security-poll-suffix=
version-string=anonymous
setuid=
setgid=
