# db/control-plane

Squashed Postgres baseline + forward migrations for the control plane
(api-next 000 §4). Lane C owns this directory (001 §5); the baseline
co-evolves with C's control-plane adapter. Postgres dialect only.
