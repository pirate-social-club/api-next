# platform-cf/config — lane A ownership

Effect `Config` + Schema providers with fail-at-startup semantics (api-next
000 §9). Lane A owns this directory as a stated exception to lane C's
platform ownership, because config schema and contracts co-evolve (001 §3).
